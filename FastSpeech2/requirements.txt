# Recommended: Python 3.10 or 3.11.
# Install PyTorch separately for the machine's CUDA version.
numpy>=1.23,<2.1
scipy>=1.9,<1.15
PyYAML>=6,<7
tqdm>=4.64,<5
librosa>=0.10,<0.12
matplotlib>=3.6,<4
scikit-learn>=1.2,<2
tgt>=1.5,<2
pyworld>=0.3.4,<0.4
Unidecode>=1.3,<2
inflect>=6,<8
g2p-en>=2.1,<3

# Required by default because TensorBoard is enabled during training.
tensorboard>=2.12,<3

# Montreal Forced Aligner is installed separately, not with this pip file.
